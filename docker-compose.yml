services:
  backend:
    build: .
    environment:
      DATABASE_URL: sqlite:////app/recruitment_app.db
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
